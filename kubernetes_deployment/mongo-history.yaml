apiVersion: apps/v1
kind: Deployment
metadata:
  name: mongo-history-deployment
  labels:
    app: mongo-history
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mongo-history
  template:
    metadata:
      labels:
        app: mongo-history
    spec:
      containers:
        - name: mongodb
          image: mongo:5.0
          ports:
            - containerPort: 27017
---
apiVersion: v1
kind: Service
metadata:
  name: mongo-history
spec:
  selector:
    app: mongo-history
  ports:
    - protocol: TCP
      port: 27017
      targetPort: 27017
